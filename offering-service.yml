server:
  port: 9003

spring:
  application:
    name: offering-service
  datasource:
    url: jdbc:mysql://localhost:3306/saloonbuddy_offering_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000
